<tr>
    <td><%= blog.title %></td>
    <td>published <%= distance_of_time_in_words(blog.created_at, Time.now) %> ago</td>
    <td><%= blog.body %></td>
    <td><%= link_to  blog.status, toggle_status_blog_path(blog), method: :post %></td> <%# we set this to a post here it is not reccomened to do this in controller as it that can allow values to easily be tampered with %>
    <td><%= link_to 'Show', blog %></td>
    <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
    <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>